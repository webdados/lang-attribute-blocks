name: Plugin Asset/Readme Update

on:
  push:
    branches:
    - main
    paths:
      # Any change on the WordPress.org plugin repository-specific assets that belong in the top-level assets directory.
      - '.wordpress-org/**'
      # The readme.txt.
      # - 'readme.txt'
      # Changes to workflow files should always verify all workflows are successful.
      - '.github/workflows/*.yml'
  # Allows you to run this workflow manually from the Actions tab.
  workflow_dispatch:

jobs:
  trunk:
    name: Push to trunk
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: WordPress.org Plugin Readme/Assets Update
      uses: 10up/action-wordpress-plugin-asset-update@stable
      env:
        SVN_PASSWORD: ${{ secrets.NAKEDCAT_WPROG_SVN_PASSWORD }}
        SVN_USERNAME: ${{ secrets.NAKEDCAT_WPROG_SVN_USERNAME }}
        SLUG: lang-attribute-blocks
        IGNORE_OTHER_FILES: true
